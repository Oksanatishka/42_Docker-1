# Create an overlay-type internal network that you will name overmind.

docker network create -d overlay --internal overmind

# Usage                     docker network create [OPTIONS] NETWORK
# docker network create     Create a network
# --driver , -d	bridge	    Driver to manage the Network
# The DRIVER accepts bridge or overlay which are the built-in network drivers.
# --internal	        	Restrict external access to the network

# To check: 
# docker network ls